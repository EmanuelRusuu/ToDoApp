export type SelectedState = {
  title: { selected: boolean; order: boolean }
  description: { selected: boolean; order: boolean }
  priority: { selected: boolean; order: boolean }
  date: { selected: boolean; order: boolean }
}
